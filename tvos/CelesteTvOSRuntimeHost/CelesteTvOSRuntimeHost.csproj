<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0-tvos</TargetFramework>
    <OutputType>Exe</OutputType>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <SupportedOSPlatformVersion>16.0</SupportedOSPlatformVersion>
    <TargetPlatformMinVersion>16.0</TargetPlatformMinVersion>
    <ApplicationId Condition="'$(ApplicationId)' == ''">com.example.celeste-tvos</ApplicationId>
    <ApplicationTitle>Celeste</ApplicationTitle>
    <ApplicationDisplayVersion>16.0</ApplicationDisplayVersion>
    <ApplicationVersion>1</ApplicationVersion>
    <AssemblyInformationalVersion>16.0.0+stage16b</AssemblyInformationalVersion>
    <CelesteLaunchMode Condition="'$(CelesteLaunchMode)' == ''">Stage2Diagnostic</CelesteLaunchMode>
    <CelesteRuntimeEnabled Condition="'$(CelesteLaunchMode)' == 'CelestePreflight' or '$(CelesteLaunchMode)' == 'Celeste' or '$(CelesteLaunchMode)' == 'CelestePrologueDiagnostic' or '$(CelesteLaunchMode)' == 'CelesteAudio' or '$(CelesteLaunchMode)' == 'CelestePersistenceDiagnostic' or '$(CelesteLaunchMode)' == 'CelestePersistenceInspect'">true</CelesteRuntimeEnabled>
    <PersistenceEnabled Condition="'$(PersistenceEnabled)' == '' and '$(CelesteRuntimeEnabled)' == 'true'">true</PersistenceEnabled>
    <PersistenceStorageNamespace Condition="'$(PersistenceStorageNamespace)' == ''">production</PersistenceStorageNamespace>
    <FmodDiagnosticEnabled Condition="'$(CelesteLaunchMode)' == 'FmodDiagnostic'">true</FmodDiagnosticEnabled>
    <CelesteAudioEnabled Condition="'$(CelesteLaunchMode)' == 'CelesteAudio'">true</CelesteAudioEnabled>
    <CelesteTvOSRepoRoot>$([System.IO.Path]::GetFullPath('$(MSBuildThisFileDirectory)..\..'))</CelesteTvOSRepoRoot>
    <TvOSHostStageRoot>$(CelesteTvOSRepoRoot)\.build\tvos-host</TvOSHostStageRoot>
    <TvOSHostNativeRoot>$(TvOSHostStageRoot)\native</TvOSHostNativeRoot>
    <CelesteRuntimeRoot Condition="'$(CelesteRuntimeRoot)' == '' and '$(PersistenceEnabled)' == 'true' and '$(CelesteAudioEnabled)' == 'true'">$(CelesteTvOSRepoRoot)\.build\celeste-runtime\stage6-current\audio</CelesteRuntimeRoot>
    <CelesteRuntimeRoot Condition="'$(CelesteRuntimeRoot)' == '' and '$(PersistenceEnabled)' == 'true'">$(CelesteTvOSRepoRoot)\.build\celeste-runtime\stage6-current\noaudio</CelesteRuntimeRoot>
    <CelesteRuntimeRoot Condition="'$(CelesteRuntimeRoot)' == '' and '$(CelesteAudioEnabled)' == 'true'">$(CelesteTvOSRepoRoot)\.build\celeste-runtime\stage5b-current</CelesteRuntimeRoot>
    <CelesteRuntimeRoot Condition="'$(CelesteRuntimeRoot)' == ''">$(CelesteTvOSRepoRoot)\.build\celeste-runtime\stage3c-current</CelesteRuntimeRoot>
    <CelesteRuntimeManagedRoot>$(CelesteRuntimeRoot)\managed</CelesteRuntimeManagedRoot>
    <CelesteRuntimeContentRoot>$(CelesteRuntimeRoot)\content\Content</CelesteRuntimeContentRoot>
    <FmodDiagnosticStageRoot Condition="'$(FmodDiagnosticStageRoot)' == ''">$(CelesteTvOSRepoRoot)\.build\fmod-tvos\current</FmodDiagnosticStageRoot>
    <FmodNativeEnabled Condition="'$(FmodDiagnosticEnabled)' == 'true' or '$(CelesteAudioEnabled)' == 'true'">true</FmodNativeEnabled>
    <CelesteBrandingEnabled Condition="'$(CelesteBrandingEnabled)' == ''">false</CelesteBrandingEnabled>
    <CelesteBrandAssetsRoot Condition="'$(CelesteBrandAssetsRoot)' == ''">$(CelesteTvOSRepoRoot)\.build\tvos-self-build\artwork\Assets.xcassets</CelesteBrandAssetsRoot>
    <AppIcon Condition="'$(CelesteBrandingEnabled)' == 'true'">App Icon</AppIcon>
    <FnaExtensionTargets>$(MSBuildThisFileDirectory)Stage3BFnaExtension.targets</FnaExtensionTargets>
    <CodesignEntitlements>Entitlements.plist</CodesignEntitlements>
    <TrimMode Condition="'$(Configuration)' == 'Release'">full</TrimMode>
    <DebugSymbols Condition="'$(Configuration)' == 'Release'">false</DebugSymbols>
    <DebugType Condition="'$(Configuration)' == 'Release'">none</DebugType>
    <TrimmerRemoveSymbols Condition="'$(Configuration)' == 'Release'">true</TrimmerRemoveSymbols>
    <MtouchLink Condition="'$(Configuration)' == 'Release'">Full</MtouchLink>
    <UseInterpreter>false</UseInterpreter>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'Stage2Diagnostic'">$(DefineConstants);STAGE2_DIAGNOSTIC</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'FmodDiagnostic' and '$(RuntimeIdentifier)' == 'tvos-arm64'">$(DefineConstants);FMOD_DIAGNOSTIC_DEVICE</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'FmodDiagnostic' and '$(RuntimeIdentifier)' != 'tvos-arm64'">$(DefineConstants);FMOD_DIAGNOSTIC_UNAVAILABLE</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelestePreflight'">$(DefineConstants);CELESTE_RUNTIME;CELESTE_PREFLIGHT</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'Celeste'">$(DefineConstants);CELESTE_RUNTIME;CELESTE_GAME</DefineConstants>
    <DefineConstants Condition="'$(PersistenceEnabled)' == 'true'">$(DefineConstants);TVOS_CELESTE_RUNTIME_HOST</DefineConstants>
    <DefineConstants Condition="'$(SaveManagerAutomation)' == 'true'">$(DefineConstants);TVOS_SAVE_MANAGER_AUTOMATION</DefineConstants>
    <DefineConstants Condition="'$(PersistenceEnabled)' == 'true' and '$(PersistenceStorageNamespace)' == 'acceptance'">$(DefineConstants);STAGE6_NAMESPACE_ACCEPTANCE</DefineConstants>
    <DefineConstants Condition="'$(PersistenceEnabled)' == 'true' and '$(PersistenceStorageNamespace)' == 'restart'">$(DefineConstants);STAGE6_NAMESPACE_RESTART</DefineConstants>
    <DefineConstants Condition="'$(PersistenceEnabled)' == 'true' and '$(PersistenceStorageNamespace)' == 'tests'">$(DefineConstants);STAGE6_NAMESPACE_TESTS</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelestePersistenceDiagnostic'">$(DefineConstants);CELESTE_RUNTIME;CELESTE_PERSISTENCE_DIAGNOSTIC</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelestePersistenceInspect'">$(DefineConstants);CELESTE_RUNTIME;CELESTE_PERSISTENCE_INSPECT</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelestePersistenceDiagnostic' and '$(Stage9BRetainedFixtureRoot)' != ''">$(DefineConstants);STAGE9B_RETAINED_FIXTURES</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelesteAudio' and '$(RuntimeIdentifier)' == 'tvos-arm64'">$(DefineConstants);CELESTE_RUNTIME;CELESTE_GAME;CELESTE_AUDIO</DefineConstants>
    <Stage5BAudioScenario Condition="'$(Stage5BAudioScenario)' == ''">normal</Stage5BAudioScenario>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelesteAudio' and ('$(Stage5BAudioScenario)' == 'prologue-normal' or '$(Stage5BAudioScenario)' == 'prologue-skip')">$(DefineConstants);CELESTE_PROLOGUE_DIAGNOSTIC</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelesteAudio' and '$(Stage5BAudioScenario)' == 'prologue-normal'">$(DefineConstants);STAGE3C_SCENARIO_NORMAL</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelesteAudio' and '$(Stage5BAudioScenario)' == 'prologue-skip'">$(DefineConstants);STAGE3C_SCENARIO_SKIP</DefineConstants>
    <Stage3CPrologueScenario Condition="'$(Stage3CPrologueScenario)' == ''">normal</Stage3CPrologueScenario>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelestePrologueDiagnostic'">$(DefineConstants);CELESTE_RUNTIME;CELESTE_GAME;CELESTE_PROLOGUE_DIAGNOSTIC</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelestePrologueDiagnostic' and '$(Stage3CPrologueScenario)' == 'normal'">$(DefineConstants);STAGE3C_SCENARIO_NORMAL</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelestePrologueDiagnostic' and '$(Stage3CPrologueScenario)' == 'skip'">$(DefineConstants);STAGE3C_SCENARIO_SKIP</DefineConstants>
    <DefineConstants Condition="'$(CelesteLaunchMode)' == 'CelestePrologueDiagnostic' and '$(Stage3CPrologueScenario)' == 'manual'">$(DefineConstants);STAGE3C_SCENARIO_MANUAL</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="LegacyXnbReaders.cs" />
    <Compile Remove="TvOSTitleContainer.cs" />
    <ProjectReference Include="..\FNA.TvOS\FNA.TvOS.csproj"
                      AdditionalProperties="CustomAfterMicrosoftCommonTargets=$(FnaExtensionTargets);PersistenceEnabled=$(PersistenceEnabled);CelesteTvOSRepoRoot=$(CelesteTvOSRepoRoot)" />
    <Compile Include="..\CelesteTvOSHost\Stage2Game.cs" Link="Stage2Diagnostic\Stage2Game.cs" />
    <Compile Include="..\CelesteTvOSHost\Stage2Log.cs" Link="Stage2Diagnostic\Stage2Log.cs" />
    <Compile Include="..\CelesteTvOSHost\NativeInteropSelfTest.cs" Link="Stage2Diagnostic\NativeInteropSelfTest.cs" />
    <Compile Include="..\CelesteTvOSHost\LifecycleMonitor.cs" Link="Stage2Diagnostic\LifecycleMonitor.cs" />
    <BundleResource Include="PrivacyInfo.xcprivacy" LogicalName="PrivacyInfo.xcprivacy" />
  </ItemGroup>

  <ItemGroup Condition="'$(CelesteBrandingEnabled)' == 'true'">
    <ImageAsset Include="$(CelesteBrandAssetsRoot)\**\*.*">
      <Link>Assets.xcassets\%(RecursiveDir)%(Filename)%(Extension)</Link>
      <Visible>false</Visible>
    </ImageAsset>
  </ItemGroup>

  <ItemGroup Condition="'$(CelesteRuntimeEnabled)' == 'true'">
    <ProjectReference Include="$(CelesteRuntimeManagedRoot)\Celeste.Modern.csproj"
                      AdditionalProperties="CelesteTvOSRepoRoot=$(CelesteTvOSRepoRoot);CelesteManagedGeneratedRoot=$(CelesteRuntimeManagedRoot);CustomAfterMicrosoftCommonTargets=$(FnaExtensionTargets);Stage6PersistenceEnabled=$(PersistenceEnabled)" />
    <ProjectReference Include="$(CelesteRuntimeManagedRoot)\Celeste.Content.Modern.csproj"
                      AdditionalProperties="CelesteManagedGeneratedRoot=$(CelesteRuntimeManagedRoot)" />
    <BundleResource Include="$(CelesteRuntimeContentRoot)\**\*"
                    LogicalName="Content\%(RecursiveDir)%(Filename)%(Extension)" />
    <TrimmerRootDescriptor Include="CelesteLinker.xml" />
  </ItemGroup>

  <!-- Optional, ignored, lawful local Stage 9A payloads. This lane exists only
       for simulator regression and is never part of a public/product app. -->
  <ItemGroup Condition="'$(CelesteLaunchMode)' == 'CelestePersistenceDiagnostic' and '$(Stage9BRetainedFixtureRoot)' != ''">
    <BundleResource Include="$(Stage9BRetainedFixtureRoot)\*.celeste"
                    LogicalName="Stage9BFixtures\%(Filename)%(Extension)" />
  </ItemGroup>

  <ItemGroup Condition="'$(FmodNativeEnabled)' == 'true' and '$(RuntimeIdentifier)' == 'tvos-arm64'">
    <BundleResource Include="$(FmodDiagnosticStageRoot)\banks\Content\FMOD\**\*.bank"
                    LogicalName="Content\FMOD\%(RecursiveDir)%(Filename)%(Extension)" />
    <NativeReference Include="$(FmodDiagnosticStageRoot)\xcframeworks\FMOD.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad>
    </NativeReference>
    <NativeReference Include="$(FmodDiagnosticStageRoot)\xcframeworks\FMODStudio.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad><IsCxx>true</IsCxx>
    </NativeReference>
    <NativeReference Include="$(FmodDiagnosticStageRoot)\xcframeworks\FMODSDL.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad>
    </NativeReference>
  </ItemGroup>

  <!-- Static imports resolve through __Internal. ForceLoad retains those
       exports; the framework list remains the accepted Stage 1 link-probe set. -->
  <ItemGroup>
    <NativeReference Include="$(TvOSHostNativeRoot)\SDL2.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad>
      <Frameworks>Foundation UIKit AVFoundation AudioToolbox CoreBluetooth CoreGraphics CoreHaptics CoreImage GameController IOSurface Metal Network OpenGLES QuartzCore</Frameworks>
    </NativeReference>
    <NativeReference Include="$(TvOSHostNativeRoot)\FNA3D.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad>
    </NativeReference>
    <NativeReference Include="$(TvOSHostNativeRoot)\FAudio.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad>
    </NativeReference>
    <NativeReference Include="$(TvOSHostNativeRoot)\Theorafile.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad>
    </NativeReference>
    <NativeReference Include="$(TvOSHostNativeRoot)\tvStubs.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad>
    </NativeReference>
    <NativeReference Include="$(TvOSHostNativeRoot)\MoltenVK.xcframework">
      <Kind>Static</Kind><SmartLink>false</SmartLink><ForceLoad>true</ForceLoad><IsCxx>true</IsCxx>
    </NativeReference>
  </ItemGroup>

  <Target Name="ValidateCelesteLaunchMode" BeforeTargets="PrepareForBuild">
    <Error Condition="'$(CelesteLaunchMode)' != 'Stage2Diagnostic' and '$(CelesteLaunchMode)' != 'CelestePreflight' and '$(CelesteLaunchMode)' != 'Celeste' and '$(CelesteLaunchMode)' != 'CelestePrologueDiagnostic' and '$(CelesteLaunchMode)' != 'FmodDiagnostic' and '$(CelesteLaunchMode)' != 'CelesteAudio' and '$(CelesteLaunchMode)' != 'CelestePersistenceDiagnostic' and '$(CelesteLaunchMode)' != 'CelestePersistenceInspect'"
           Text="CelesteLaunchMode must be Stage2Diagnostic, CelestePreflight, Celeste, CelestePrologueDiagnostic, FmodDiagnostic, CelesteAudio, CelestePersistenceDiagnostic, or CelestePersistenceInspect." />
    <Error Condition="'$(PersistenceEnabled)' == 'true' and '$(PersistenceStorageNamespace)' != 'production' and '$(PersistenceStorageNamespace)' != 'acceptance' and '$(PersistenceStorageNamespace)' != 'restart' and '$(PersistenceStorageNamespace)' != 'tests'"
           Text="PersistenceStorageNamespace must be one of the committed fixed categories: production, acceptance, restart, or tests." />
    <Error Condition="'$(CelesteLaunchMode)' == 'CelestePersistenceDiagnostic' and '$(PersistenceStorageNamespace)' != 'tests'"
           Text="CelestePersistenceDiagnostic must use PersistenceStorageNamespace=tests." />
    <Error Condition="'$(CelesteLaunchMode)' == 'CelestePersistenceInspect' and '$(PersistenceStorageNamespace)' != 'production'"
           Text="CelestePersistenceInspect is read-only and must use the production namespace." />
    <Error Condition="'$(CelesteAudioEnabled)' == 'true' and '$(RuntimeIdentifier)' != 'tvos-arm64'"
           Text="CelesteAudio is device-only. Use Celeste for the arm64 simulator no-audio lane." />
    <Error Condition="'$(CelesteAudioEnabled)' == 'true' and '$(Stage5BAudioScenario)' != 'normal' and '$(Stage5BAudioScenario)' != 'prologue-normal' and '$(Stage5BAudioScenario)' != 'prologue-skip'"
           Text="Stage5BAudioScenario must be normal, prologue-normal, or prologue-skip." />
    <Error Condition="'$(CelesteLaunchMode)' == 'CelestePrologueDiagnostic' and '$(Stage3CPrologueScenario)' != 'normal' and '$(Stage3CPrologueScenario)' != 'skip' and '$(Stage3CPrologueScenario)' != 'manual'"
           Text="Stage3CPrologueScenario must be normal, skip, or manual." />
    <Error Condition="'$(CelesteBrandingEnabled)' == 'true' and !Exists('$(CelesteBrandAssetsRoot)\App Icon.brandassets\Contents.json')"
           Text="Local Celeste tvOS artwork is missing. Run scripts/generate-celeste-tvos-artwork.sh." />
    <Error Condition="!Exists('$(TvOSHostStageRoot)\normalized-manifest.json')"
           Text="Prepared native inputs are missing. Run scripts/prepare-tvos-host-native.sh." />
    <Error Condition="'$(CelesteRuntimeEnabled)' == 'true' and !Exists('$(CelesteRuntimeManagedRoot)\Celeste.Modern.csproj')"
           Text="Generated Celeste managed input is missing. Run scripts/prepare-celeste-tvos-runtime.sh." />
    <Error Condition="'$(CelesteRuntimeEnabled)' == 'true' and !Exists('$(CelesteRuntimeContentRoot)\Effects\Border.xnb')"
           Text="Generated Celeste Content input is missing. Run scripts/prepare-celeste-tvos-runtime.sh without --managed-only." />
    <Error Condition="'$(CelesteRuntimeEnabled)' == 'true' and Exists('$(CelesteRuntimeContentRoot)\FMOD')"
           Text="Generated Celeste Content must not contain FMOD; device audio banks come only from accepted FMOD staging." />
    <Error Condition="'$(FmodNativeEnabled)' == 'true' and '$(RuntimeIdentifier)' == 'tvos-arm64' and !Exists('$(FmodDiagnosticStageRoot)\logical-manifest.json')"
           Text="Prepared FMOD inputs are missing. Run scripts/prepare-fmod-tvos.sh." />
    <Error Condition="'$(FmodNativeEnabled)' == 'true' and '$(RuntimeIdentifier)' == 'tvos-arm64' and !Exists('$(FmodDiagnosticStageRoot)\banks\Content\FMOD\Desktop\Master Bank.bank')"
           Text="Prepared FMOD banks are missing." />
    <Error Condition="'$(CelesteAudioEnabled)' == 'true' and !Exists('$(CelesteRuntimeManagedRoot)\Celeste\TvOSStage5BAudioBridge.cs')"
           Text="CelesteAudio requires the separately generated real-audio managed tree." />
    <Error Condition="'$(PersistenceEnabled)' == 'true' and !Exists('$(CelesteRuntimeManagedRoot)\Celeste\TvOSStage6PersistenceHooks.cs')"
           Text="tvOS persistence requires scripts/prepare-celeste-tvos-stage6.sh output." />
    <Error Condition="'$(PersistenceEnabled)' == 'true' and !Exists('$(CelesteRuntimeManagedRoot)\Celeste\TvOSSaveManagerBridge.cs')"
           Text="Save Manager requires scripts/prepare-celeste-tvos-stage6.sh output." />
    <Error Condition="'$(PersistenceEnabled)' == 'true' and !Exists('$(CelesteRuntimeManagedRoot)\Celeste\TvOSControllerPromptBridge.cs')"
           Text="Controller Prompts requires scripts/prepare-celeste-tvos-stage6.sh output." />
    <Error Condition="'$(PersistenceEnabled)' == 'true' and !Exists('$(CelesteRuntimeManagedRoot)\Celeste\TvOSQuitBridge.cs')"
           Text="Graceful Quit requires scripts/prepare-celeste-tvos-stage6.sh output." />
    <Error Condition="'$(PersistenceEnabled)' == 'true' and !Exists('$(CelesteRuntimeManagedRoot)\Celeste\TvOSPerformanceHudBridge.cs')"
           Text="Performance HUD requires scripts/prepare-celeste-tvos-stage6.sh output." />
  </Target>

  <!-- Apple workload incremental builds share one app-bundle directory across
       launch modes. Remove the only mode-exclusive proprietary resource before
       bundle assembly so a prior CelesteAudio publish cannot contaminate a
       no-audio simulator or persistence-diagnostic package. -->
  <Target Name="RemoveStaleFmodBanksFromNoAudioBundle"
          BeforeTargets="_CreateAppBundle"
          Condition="'$(FmodNativeEnabled)' != 'true' and '$(_AppBundlePath)' != ''">
    <RemoveDir Directories="$(_AppBundlePath)\Content\FMOD" />
  </Target>

  <Import Project="..\Local.Build.props" Condition="Exists('..\Local.Build.props')" />
</Project>
