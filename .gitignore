_celeste_game/
_celeste_decomp/
celeste/

# Generated Stage 1 tvOS native sources, intermediates, logs and packages.
.build/tvos-native/
artifacts/tvos-native/

# Generated Stage 2 host inputs, app bundles, logs, screenshots, and local signing.
.build/tvos-host/
.build/tvos-dotnet/
artifacts/tvos-host/
tvos/Local.Build.props
tvos/**/bin/
tvos/**/obj/

# User-owned Celeste input, generated/decompiled managed source, build output,
# diagnostics, and local repository-scoped tool/package state for Stage 3A.
.build/celeste-managed/
artifacts/celeste-managed/

# User-owned Stage 3B runtime content, regenerated managed source, app bundles,
# runtime logs, screenshots, manifests, and device evidence.
.build/celeste-runtime/
artifacts/celeste-runtime/

# User-supplied FMOD SDK staging, banks, bridge builds, diagnostic apps, and
# physical-device evidence. Proprietary inputs and private logs stay local.
.build/fmod-tvos/
artifacts/fmod-tvos/

# Generated Stage 8 self-build configuration, local artwork, application
# bundles, IPAs, signing evidence, and privacy-safe user-facing outputs.
.build/tvos-self-build/
artifacts/tvos-self-build/
dist/

# Generated Stage 10A full-AOT automation app. This contains regenerated
# user-owned Celeste assemblies, content, FMOD banks, and private device logs.
.build/tvos-stage10a-automation/

# Generated Stage 10B regeneration logs and full-AOT acceptance apps. These
# contain regenerated Celeste assemblies, game content, FMOD banks, and local
# device/signing evidence and must never become repository candidates.
.build/stage10b-*.log
.build/tvos-stage10b-*.log
.build/tvos-stage10b-*/

.DS_Store
